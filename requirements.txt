# Core
langchain-core==1.6.3          # Messages + tool binding
langchain-mistralai==0.2.12    # Mistral chat model with tool calling (free Experiment plan works)
langgraph==1.2.11              # StateGraph orchestration + interrupt() for human approval
langgraph-checkpoint-sqlite==3.1.1  # Persists paused runs so they can be resumed from another process
pydantic>=2.0                  # Data validation

# RAG
chromadb>=0.5.0                # Local vector store
sentence-transformers          # Embeddings (all-MiniLM-L6-v2)
rank-bm25                      # Keyword fallback when embeddings are unavailable

# UI
streamlit>=1.35.0              # Web UI
rich>=13.0                     # CLI formatting

# Testing
pytest>=8.0

# Config
python-dotenv>=1.0
